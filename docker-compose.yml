services:
  pasteboard:
    image: dillonreese/pasteboard:latest
    ports:
      - "3000:3001"
    volumes:
      - ./uploads:/app/uploads
      - ./server/pasteboard.db:/app/pasteboard.db
    environment:
      - NODE_ENV=production
